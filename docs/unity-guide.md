# Unity Guide

## Import Steps
1. Install the package `com.actoratect.layout-sync`.
2. Open **Tools > Layout Sync > Import Layout JSON**.
3. Pick the exported JSON file.

Objects are created under a root GameObject named `LayoutImport`.
